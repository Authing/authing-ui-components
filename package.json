{
  "name": "root",
  "private": true,
  "scripts": {
    "build:lib": "lerna run build:lib --stream",
    "build:demo": "node scripts/buildDemo.js",
    "publish:lib": "lerna publish from-package",
    "publish:rc": "lerna publish from-package --canary --pre-dist-tag=rc --preid=rc",
    "build:publish": "lerna version && yarn build:lib && yarn publish:lib",
    "build:publish:rc": "lerna version && yarn build:lib && yarn publish:rc",
    "clean": "lerna clean",
    "postinstall": "node scripts/postInstall.js",
    "prebuild:lib": "npm run postinstall"
  },
  "workspaces": [
    "packages/angular-components/projects/ng-ui-components",
    "packages/angular-components",
    "packages/react-components",
    "packages/vue-components",
    "packages/native-js"
  ],
  "devDependencies": {
    "lerna": "^3.22.1",
    "replace-in-file": "^6.2.0",
    "shelljs": "0.8.4"
  },
  "npmClient": "yarn",
  "dependencies": {}
}